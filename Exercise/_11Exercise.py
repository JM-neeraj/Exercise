# Same block with a same indentation (four spaces).
# Examine what will happen if you put less spaces (e.g., three spaces) or more spaces (e.g., five spaces) in line 9, and run the program."

x = 2

rnew = x

for i in range(10):
   r1 = rnew  # 3 spaces
    # r2 = x / r1  # 4 spaces

# IndentationError: unexpected indent